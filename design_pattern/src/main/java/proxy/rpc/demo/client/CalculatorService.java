package proxy.rpc.demo.client;

/**
 * Define a local interface class, just the same as the remote service interface class.
 *
 * @author xuxiang
 */
public interface CalculatorService {
    int add(int a, int b);
}
