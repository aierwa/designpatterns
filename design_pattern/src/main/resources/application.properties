
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://10.15.3.4:3306/fsis-bms
spring.datasource.username=root
spring.datasource.password=!#%qetADG135

# mapper xml 放在 resources 下面的配置
mybatis.mapper-locations=classpath:mapping/*Mapper.xml
# mapper xml 放在和 Mapper 接口同级目录里面时，这种要在 maven 打包配置将 xml 进行打包才行
#mybatis.mapper-locations=classpath*:practice/mybatis/mapper/*.xml